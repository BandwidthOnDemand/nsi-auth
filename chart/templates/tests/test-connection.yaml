apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsi-auth.fullname" . }}-test-connection"
  labels:
    {{- include "nsi-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nsi-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
